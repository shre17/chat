<li>
  <div class="row">
    <div class="<%= user.id == message.user_id ? 'message-sent' : 'message-received' %>">
      <%#= message.body %>
      <%= "#{content_with_emoji(message.body)}".html_safe %>
    </div>
  </div>
</li>